'use client';

export function MobileLanding() {
  return (
    <div className="flex items-center justify-center min-h-screen bg-[var(--background)] p-4">
      <div className="text-center">
        <h1 className="text-[var(--foreground)] font-[var(--font-mono)] text-sm mb-4">
          WELCOME TO SONGS FOR THE ROAD
        </h1>
        <p className="text-[var(--foreground)] font-[var(--font-mono)] text-sm">
          VISIT ON DESKTOP TO ADD MEMORIES
        </p>
      </div>
    </div>
  );
}
